web:
  extends:
    file: docker-common.yml
    service: web
  volumes:
   - .:/code:ro
#  links:
#   - redis
  environment:
    - NODE_PATH=/my_app/node_modules    
#    - REDIS_HOST=redis
  command: /env/bin/python /code/main.py
#redis:
#  image: redis
